import XCTest
@testable import PetScoutOnboarding

final class PetScoutOnboardingTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PetScoutOnboarding().text, "Hello, World!")
    }
}
